# Findings — what still needs to move (as at 2026-09-22 12:30 IST)

Method: full set comparison, not a timestamp cutoff. Every v1 `manageLeads` row on forms
100/109 was checked against `v2_leads.v1_lead_id`, and every v1 `ApplicationManager` row on
those forms against `v2_leads.v1_application_id`. Script: `scripts/01-gap-analysis.cjs`.

## Headline reconciliation

| | count |
|---|---|
| v1 UG `manageLeads` (forms 100 + 109) | 71,331 |
| of those, already in v2 | 71,166 |
| **missing from v2** | **165** |
| v1 UG `ApplicationManager` | 2,421 |
| of those, already in v2 | 2,331 |
| **missing from v2** | **90** |
| v2 UG rows with no v1 link (born in v2) | 2,654 |
| v1 lead ids mapped to more than one v2 row | 0 |
| v2 rows for UG v1 leads sitting outside UG scope | 0 |

The last two lines matter: the existing migration has **no duplicates and no misplacement**.

## Stream A — 165 missing leads

All 165 are **live** in v1 (`isLeadDeleted = false`). Their split:

- by form: 27 → form 104 (SoB), 138 → form 105 (DSAI)
- by created date IST: 23 Jul ×1, 2 Sep ×1, **17 Sep ×91, 18 Sep ×52, 19 Sep ×8,
  20 Sep ×6, 21 Sep ×6**
- by source: Collegewollege 73, Dekhocampus 50, collegedekho 15, Getmyuni 14, whatsapp 5,
  Shiksha 3, Kollegeapply 2, Zollege 1, Purge-K-12 1, Allen Grade 12 data 1

This confirms the standing diagnosis: **vendor feeds and the WhatsApp chatbot still write
only to v1**, while the website and ads write only to v2. The two stragglers (23 Jul,
2 Sep) are offline/CSV uploads that the sync missed at the time.

Every mapping input resolves:
- 8 distinct `assignTo` counsellors, all present in the observed counsellor map
- stages used: 158 ×159, 165 ×4, 159 ×2 — all map cleanly
- sub-stages: null ×163, 349 ×1, 344 ×1 — both map
- 163 have `userId` NULL (pure leads); **2 are `userType='applicant'`** (see Stream C)

## Stream B — satellites for those 165

| | count |
|---|---|
| `UserTimelines` → `timelines` | 426 (over all 165 leads) |
| `Notes` → `notes` | 1 |
| `manageLeads.tags` → `lead_tags` | 1 (`W/A Clicks Data` → v2 `tags.id` 286) |
| `under_graduate` rows | 165 (city on 158, state on 1) |
| `StudentTimelines`, `councellorCalls`, `leadCallLogs` | 0 |

Timeline event types present: Counsellor_Assigned 167, Lead Added 163, Lead Type Updated 31,
whatsapp 24, Email_Sent 23, Lead Synced to CAPI 7, Changed_Lead_Stage 6, Stage Assigned 2,
Email Sent 2, Application Stage Changed 1.

`Stage Assigned` ×2 is **not** in the observed event-type lookup — it never occurred in the
already-migrated data. Needs a decision (see `04-decisions.md`).

4 timelines reference `leadStageId = 413`, a **PGP UI UX** stage (school 41), not a UG one.

## Stream C — 3 genuinely new applications

These have a `manageLeads` row, so they are real, current applications:

| v1 app | v1 lead | person | form | submitted | payment | note |
|---|---|---|---|---|---|---|
| 730633 | 2498205 | Ishu Gupta | 100 | no | pending | lead also missing — in Stream A |
| 730634 | 2498215 | Ishu Gupta | 109 | yes | pending | lead also missing — in Stream A |
| 730635 | 2417827 | Ananya Gupta | 100 | **yes** | **completed** | **lead already in v2** (id 700791) as a plain lead |

**730635 is the urgent one**: the applicant paid on 21 Sep, and in v2 they still look like
an untouched lead. This is an UPDATE of an existing v2 row, not an insert.

Migrating Ishu Gupta also requires creating a v2 `users` row (v1 user 722827 has no v2
counterpart), because `v2_leads.user_id` → `users.id`.

## Stream D — 87 orphan applications (pre-existing, NOT from this window)

87 of the 90 missing applications have **no `manageLeads` row at all** (`ml_count = 0`).
The previous migration read applications through `manageLeads JOIN ApplicationManager`, so
it could never see them. 83 are soft-deleted in v1. **4 are live**:

| v1 app | form | created | status | submitted |
|---|---|---|---|---|
| 712790 | 100 | 2026-07-15 | 100 | yes |
| 720980 | 100 | 2026-07-27 | 75 | yes |
| 730303 | 109 | 2026-09-10 | untouched | no |
| 730304 | 109 | 2026-09-10 | untouched | no |

This is the same structural gap recorded in the 18 Sep audit. It is **not** caused by the
recent window and is arguably a separate piece of work.

## Stream E — drift on already-migrated rows

Of the 71,166 migrated UG leads, 9,347 have been edited in v1 since their v2 snapshot, but
only **40** differ in a field worth syncing:

| field | rows |
|---|---|
| source | 34 |
| mobile | 6 |
| lead_type | 4 |
| email | 4 |
| became an applicant in v1 (the Ananya case) | 1 |

Stage and counsellor drift was deliberately excluded: **v2 is the system of record** for
those now (counsellors work in v2), so v1 changes there must NOT be pushed back.

## Duplicate-person risk (faithful to v1, but worth knowing)

Migrating Stream A creates a second row for the same person in 3 cases, because v1 itself
holds the duplicate or the person also came in through v2:

- `9662304301` Tilak Jayesh Chudasama — v1 has two leads (Clicks-Data already migrated,
  Allen Grade 12 data not); both on form 109
- `dhairyakohli1424@gmail.com` Dhairya Kohli — vendor lead in v1 (Shiksha) while the same
  person already applied directly in v2 on form 104
- `8121283111` — same mobile, two genuinely different people; not a real duplicate
